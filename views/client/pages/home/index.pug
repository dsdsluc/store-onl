extends ../../layouts/default.pug
include ../../mixins/box-head.pug
include ../../mixins/products-grid.pug

block main 
    h1 Trang chu
    .container.mb-3 
        .row.mb-3 
            .col-12 
               +box-head("Sản phẩm nổi bật") 
        +products-grid(products)
        .row.mb-3 
            .col-12 
               +box-head("Sản phẩm mới nhất") 
        +products-grid(recentProducts)
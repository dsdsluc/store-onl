extends ../../layouts/default.pug
include ../../mixins/box-head.pug
include ../../mixins/alert.pug

block main
  +alert-success(5000)
  +alert-error(5000)
  .container.my-3
    include ../../partials/list-btn-user.pug
    .row
      .col-12
        +box-head("Danh sách người dùng")

    .row(data-users-not-friend = user.id) 
        each userNotFriend in users
            .col-6(user_id = userNotFriend.id)
                div(class='box-user')
                    div(class="inner-avatar")
                        img(src =(userNotFriend.avatar ? userNotFriend.avatar : "/client/images/user-avatar.jpg"), alt= userNotFriend.fullName)
                    div(class="inner-infor")
                        div(class="inner-name") #{userNotFriend.fullName}  
                        div(class="inner-buttons") 
                            button(
                                class="btn btn-sm btn-primary me-md-2"
                                btn-add-friend=userNotFriend.id
                                ) Kết bạn
                            button(
                                class="btn btn-sm btn-secondary me-md-2"
                                btn-cancel-friend=userNotFriend.id
                                ) Hủy
            
block script 
    script(src="/client/js/user.js")